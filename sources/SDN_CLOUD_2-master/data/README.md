# Data folder

This folder is used for generated artifacts:

- `synthetic_sdn_dataset.csv` - synthetic labeled traffic data used for model training
- model files are written to `ml/models/`

The project intentionally generates synthetic training data locally so the bundle remains self-contained and deployable without downloading external datasets.
